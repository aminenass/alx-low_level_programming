#include "main.h"
/**
* _abs - This is a funtion print value
* @n : represent the parameter num
* Return : return n or -n depend either pos or neg
*/
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
